apiVersion: v1
kind: Service
metadata:
  name: {{ include "vihren.fullname" . }}
  labels:
    {{- include "vihren.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "vihren.selectorLabels" . | nindent 4 }}
